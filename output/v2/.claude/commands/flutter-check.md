# Flutter Check

린트 + 포맷 + 테스트를 한 번에 실행한다.

## Usage

`/flutter-check`

## Arguments

$ARGUMENTS

## Behavior

```bash
dart analyze && dart format --set-exit-if-changed . && flutter test --coverage
```
